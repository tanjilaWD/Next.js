import React from 'react';

const page = () => {
    throw new error('There was a huge error')
    return (
         <mail>
      <div>This is about page</div>
   </mail>
    );
};

export default page;