import React from 'react';

const page = () => {
    // throw new error('There was a huge error')
    return (
         <main>
      <div>This is about page</div>
   </main>
    );
};

export default page;